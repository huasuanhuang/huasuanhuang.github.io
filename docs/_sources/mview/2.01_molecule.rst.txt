========
分子结构
========

MView可用于分子结构文件的格式转换。

支持的输入格式有：
Gaussian输出文件（.out或.log），Dmol3输出文件（.outmol），
.fch，.mol，.gjf，.pdb，.xsd。

支持的输出格式有：
.mol，.mol2，.pdb，.xyz，.gjf。

常见分子结构格式介绍
====================

待补充。
